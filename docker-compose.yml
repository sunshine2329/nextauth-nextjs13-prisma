version: '3'
services:
  postgres:
    image: postgres:latest
    container_name: postgres
    ports:
      - '6500:5432'
    volumes:
      - progresDB:C:\Program Files\PostgreSQL\15\data
    env_file:
      - ./.env
volumes:
  progresDB:
